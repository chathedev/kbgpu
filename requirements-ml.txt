# Heavy ML dependencies. Pinned to avoid pip's resolver backtracking:
# pyannote.audio latest wants torch>=2.8 which conflicts with our torch
# 2.4.x cu124 base. We pin to a version that still works with torch 2.4.
#
# Dockerfile installs torch/torchaudio cu124 in a previous layer; these
# packages are installed with the legacy resolver to avoid exponential
# backtracking.

faster-whisper==1.1.1
numpy==1.26.4
soundfile==0.12.1
scipy==1.13.1
scikit-learn==1.5.2

# Pyannote + its lightning-based deps pinned to versions known to work
# with torch 2.4 / CUDA 12.4.
pyannote.audio==3.3.2
lightning==2.4.0
torchmetrics==1.4.2

# HF + transformers pinned. Newer transformers pulls in torch 2.6+.
transformers==4.45.2
huggingface_hub==0.26.2
tokenizers==0.20.3
safetensors==0.4.5

# DiariZen runtime deps.
einops==0.8.0
asteroid-filterbanks==0.4.0
omegaconf==2.3.0
